name: tag-commit
description: Tag the latest commit with the version read from 'bump-my-version'

branding:
  icon: activity
  color: gray-dark

inputs:
  token:
    description: GitHub Personal Access Token
    default: ${{ github.token }}
  user-name:
    description: "'git' user name"
    default: github-actions-bot
  user-email:
    description: "'git' user email"
    default: noreply@github.com
  major-minor:
    description: Add the 'major.minor' tag
    default: false
  major:
    description: Add the 'major' tag
    default: false
  latest:
    description: Add the 'latest' tag
    default: false
  dry-run:
    description: Dry run the CLI
    default: false

runs:
  using: composite
  steps:
    - name: Check out repo
      uses: actions/checkout@v6
      with:
        fetch-depth: 0

    - name: Install 'uv'
      uses: astral-sh/setup-uv@v7
      with:
        python-version: 3.13

    - name: Install 'bump-my-version'
      run: uv tool install --reinstall bump-my-version
      shell: bash

    - name: Install 'dycw-tag-commit'
      run: uv tool install --reinstall dycw-tag-commit
      shell: bash

    - name: Run 'tag-commit'
      env:
        APP_TOKEN: ${{ inputs.token }}
        APP_USER_NAME: ${{ inputs.user-name }}
        APP_USER_EMAIL: ${{ inputs.user-email }}
        APP_MAJOR_MINOR: ${{ inputs.major-minor }}
        APP_MAJOR: ${{ inputs.major }}
        APP_LATEST: ${{ inputs.latest }}
        APP_DRY_RUN: ${{ inputs.dry-run }}
      run: uv tool run --from dycw-tag-commit --isolated tag-commit
      shell: bash
