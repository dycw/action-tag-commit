name: push

on:
  push:
    branches:
      - master

jobs:
  tag:
    runs-on: ubuntu-latest
    steps:
      - name: Tag latest commit
        uses: dycw/action-tag-commit@0.1.8
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
